/**
 * تعليمات التكامل الشامل للنظام المالي
 * Comprehensive Financial Integration Guide
 * 
 * هذا الملف يحتوي على جميع الخطوات المطلوبة لتطبيق النظام المالي المتكامل
 * الذي يربط جميع العمليات (عيادات + مختبر + مصروفات + وردية) بقيود محاسبية تلقائية
 */

// =============================================================
// الخطوة 1: تحديث قاعدة البيانات (Database Migration)
// =============================================================

/*
تشغيل الملف التالي على قاعدة البيانات:
  - www/pos/admin/config/financial_tables.sql

هذا سينشئ:
1. جدول rpos_accounts (الحسابات)
2. جدول rpos_fiscal_years (السنوات المالية)
3. جدول rpos_journal_entries (قيود اليومية)
4. جدول rpos_journal_items (أطراف القيود)
5. جدول rpos_settings (الإعدادات)
6. جداول أخرى مع البيانات الافتراضية
*/

-- تشغيل هذا الملف في phpMyAdmin:
-- File: www/pos/admin/config/financial_tables.sql

// =============================================================
// الخطوة 2: تحديث الملفات الأساسية بالاستيراد
// =============================================================

/*
أضف السطر التالي إلى بداية كل ملف:
*/

// في doctor_appointments.php (بعد checklogin.php):
include_once('config/financial_helpers.php');

// في patient.php (بعد checklogin.php):
include_once('config/financial_helpers.php');

// في expenses.php (بعد checklogin.php):
include_once('config/financial_helpers.php');

// في _head.php (قبل </head>):
<script src="config/financial_tooltips.js"></script>

// =============================================================
// الخطوة 3: تحديث doctor_appointments.php
// =============================================================

/*
اتبع التعليمات في الملف:
  www/pos/admin/config/doctor_appointments_updates.php

المتغيرات:
1. أضف shift_id عند إنشاء موعد
2. استخدم دالة recordRevenueEntry() عند الدفع
3. احفظ journal_entry_id في الموعد
4. في معالج التحديث، أنشئ قيد جديد عند تغيير الحالة من unpaid إلى paid
*/

// =============================================================
// الخطوة 4: تحديث patient.php (طلبات المختبر)
// =============================================================

/*
اتبع التعليمات في الملف:
  www/pos/admin/config/patient_lab_updates.php

المتغيرات:
1. أضف shift_id عند إنشاء طلب
2. استخدم دالة recordRevenueEntry() عند الدفع
3. احفظ journal_entry_id في الطلب
4. في معالج التحديث، أنشئ قيد جديد عند تغيير الحالة من unpaid إلى paid
*/

// =============================================================
// الخطوة 5: الوصول إلى صفحة الإعدادات المالية
// =============================================================

/*
الصفحة الجديدة:
  www/pos/admin/financial_settings.php

الميزات:
1. تحديد حساب الخزنة الافتراضي
2. تحديد حساب إيرادات العيادات
3. تحديد حساب إيرادات المختبر
4. تحديد حساب العهدة الافتتاحية
5. تحديد حساب العجز والزيادة

كيفية الوصول:
- أضف لينك في السايدبار إلى financial_settings.php
*/

// =============================================================
// الخطوة 6: الملفات المرجعية (Reference Files)
// =============================================================

/*
الملفات الجديدة الكاملة:

1. config/financial_helpers.php
   - دوال الربط المحاسبي
   - createJournalEntry()
   - recordRevenueEntry()
   - recordExpenseEntry()
   - recordShiftClosureEntry()
   - getDefaultAccount()
   - helpTooltip()
   - infoAlert()

2. config/financial_tables.sql
   - جداول قاعدة البيانات
   - الحسابات الافتراضية
   - الإعدادات الأولية

3. config/financial_tooltips.js
   - نظام التنبيهات والـ Tooltips
   - auto-hide بعد 10 دقائق
   - showToast()
   - showFinancialMessage()
   - notifyFinancialTransaction()

4. financial_settings.php
   - صفحة إدارة الإعدادات المالية
   - واجهة تعريف الحسابات الافتراضية
*/

// =============================================================
// الخطوة 7: تدفق العملية (Process Flow)
// =============================================================

/*
عند إنشاء موعد في العيادة:
1. فحص وردية مفتوحة ✓
2. إدراج الموعد مع shift_id ✓
3. إذا تم الدفع (amount_paid > 0):
   a. تسجيل القيد المحاسبي:
      - مدين: حساب الخزنة
      - دائن: حساب إيرادات العيادات
   b. حفظ entry_id في الموعد
   c. عرض رسالة نجاح مع الشرح
4. إذا لم يتم الدفع:
   - عرض رسالة تنبيه (سينشأ القيد عند الدفع لاحقاً)

عند إنشاء طلب مختبر:
1. فحص وردية مفتوحة ✓
2. إدراج الطلب مع shift_id ✓
3. إذا تم الدفع (amount_paid > 0):
   a. تسجيل القيد المحاسبي:
      - مدين: حساب الخزنة
      - دائن: حساب إيرادات المختبر
   b. حفظ entry_id في الطلب
   c. عرض رسالة نجاح
4. إذا لم يتم الدفع:
   - عرض رسالة تنبيه

عند إضافة مصروف:
- الملف expenses.php بالفعل يفعل هذا بشكل صحيح ✓

عند إغلاق وردية:
- الملف _topnav.php و shift_management.php بالفعل يفعل هذا بشكل صحيح ✓
*/

// =============================================================
// الخطوة 8: الرسائل التوضيحية (Explanatory Texts)
// =============================================================

/*
أنواع الرسائل:

1. Tooltips صغيرة (ظهور عند التمرير):
<?php echo helpTooltip("هذا الحساب يستخدم لتسجيل جميع المبيعات", "top"); ?>

2. Alert Boxes (رسائل توضيحية رئيسية):
<?php echo infoAlert("عند إنشاء موعد، يتم إنشاء قيد محاسبي تلقائي يربط إيراد العيادة بخزنة النقدية", "info"); ?>

3. رسائل JavaScript (في AJAX):
showToast('تم حفظ القيد المحاسبي #123', 'success', 5000);
showFinancialMessage('إيراد عيادة مسجل', 'تم تسجيل دفع عيادة وربطه بالقيد المحاسبي', 10*60*1000);

4. إشعارات العمليات:
notifyFinancialTransaction('clinic', 150, 'إيراد موعد طبي');
*/

// =============================================================
// الخطوة 9: التحقق من الأعمدة (Verify Database Schema)
// =============================================================

/*
التأكد من وجود الأعمدة التالية:

جدول rpos_appointments:
- app_id (موجود)
- appointment_code (موجود)
- shift_id (يجب التأكد من وجوده)
- journal_entry_id (جديد - يجب إضافته)

جدول rpos_lab_requests:
- req_id (موجود)
- req_code (موجود)
- shift_id (يجب التأكد من وجوده)
- journal_entry_id (جديد - يجب إضافته)

أوامر SQL للتأكد:
ALTER TABLE rpos_appointments ADD COLUMN shift_id VARCHAR(50) DEFAULT NULL;
ALTER TABLE rpos_appointments ADD COLUMN journal_entry_id INT DEFAULT NULL;
ALTER TABLE rpos_lab_requests ADD COLUMN shift_id VARCHAR(50) DEFAULT NULL;
ALTER TABLE rpos_lab_requests ADD COLUMN journal_entry_id INT DEFAULT NULL;
*/

// =============================================================
// الخطوة 10: الاختبار والتحقق (Testing Checklist)
// =============================================================

/*
اختبارات يجب تنفيذها:

1. [] تشغيل financial_tables.sql بدون أخطاء
2. [] فتح financial_settings.php وتحديد جميع الحسابات الافتراضية
3. [] إنشاء موعد عيادة مع دفع - التحقق من:
   - إنشاء الموعد ✓
   - إنشاء قيد محاسبي ✓
   - تحديث رصيد الحسابات ✓
   - عرض رسالة النجاح ✓

4. [] إنشاء طلب مختبر مع دفع - التحقق من:
   - إنشاء الطلب ✓
   - إنشاء قيد محاسبي ✓
   - تحديث رصيد الحسابات ✓
   - عرض رسالة النجاح ✓

5. [] إضافة مصروف - التحقق من:
   - إنشاء المصروف ✓
   - إنشاء قيد محاسبي ✓
   - تحديث الأرصدة ✓

6. [] إغلاق وردية - التحقق من:
   - حساب الخزنة يحتوي على جميع الإيرادات ✓
   - جميع الأرصدة صحيحة ✓
   - لا توجد أخطاء محاسبية ✓

7. [] الـ Tooltips:
   - الظهور عند التمرير ✓
   - الاختفاء بعد 10 دقائق ✓

8. [] الـ Alerts:
   - الظهور عند العملية ✓
   - الاختفاء التلقائي ✓
*/

// =============================================================
// الخطوة 11: قائمة السايدبار (Sidebar Links)
// =============================================================

/*
أضف هذا اللينك للسايدبار في _sidebar.php:

<li class="nav-item">
    <a class="nav-link" href="financial_settings.php">
        <i class="fas fa-cog mr-2"></i>
        الإعدادات المالية
    </a>
</li>

أو أضفه تحت قسم إداري:

<li class="nav-item">
    <a class="nav-link" href="finance_dashboard.php">
        <i class="fas fa-chart-pie mr-2"></i>
        الدليل المحاسبي
    </a>
</li>

<li class="nav-item">
    <a class="nav-link" href="financial_settings.php">
        <i class="fas fa-cog mr-2"></i>
        إعدادات الحسابات
    </a>
</li>
*/

// =============================================================
// الخطوة 12: ملاحظات مهمة (Important Notes)
// =============================================================

/*
1. التوازن المحاسبي:
   - يجب أن يكون المدين = الدائن دائماً
   - النظام يفحص هذا تلقائياً قبل الحفظ

2. الترجمة:
   - جميع الرسائل باللغة العربية
   - استخدم UTF-8 في جميع الملفات

3. الأمان:
   - استخدام Prepared Statements ✓
   - التحقق من الجلسات ✓
   - التحقق من الأدوار ✓

4. الأداء:
   - استخدام Transactions للعمليات المتعددة ✓
   - فهرسة الجداول الكبيرة
   - تحديث الأرصدة تلقائياً

5. النسخ الاحتياطي:
   - نسخ احتياطي دوري قبل التطبيق
   - اختبار في بيئة منفصلة أولاً
*/

// =============================================================
// الخطوة 13: المتطلبات الإضافية (Additional Requirements)
// =============================================================

/*
1. قد تحتاج لتحديث _head.php لإضافة السطر:
   <script src="config/financial_tooltips.js"></script>

2. قد تحتاج لتحديث _sidebar.php لإضافة لينك إلى financial_settings.php

3. قد تحتاج لتحديث doctor_appointments.php بشكل يدوي:
   - إضافة include('config/financial_helpers.php')
   - تحديث معالج add_appointment
   - تحديث معالج update_payment_status (إذا كان موجوداً)

4. قد تحتاج لتحديث patient.php بشكل يدوي:
   - إضافة include('config/financial_helpers.php')
   - تحديث معالج request_lab_test
   - تحديث معالج update_lab_payment (إذا كان موجوداً)
*/

// =============================================================
// الخطوة 14: التوثيق والمراجع (Documentation)
// =============================================================

/*
للمزيد من المعلومات عن:

1. القيود المحاسبية:
   - انظر financial_helpers.php - دالة createJournalEntry()

2. الإعدادات الافتراضية:
   - انظر financial_helpers.php - دالة getDefaultAccount()

3. الـ Tooltips والـ Alerts:
   - انظر financial_tooltips.js - جميع الدوال المتعلقة

4. معالجات AJAX:
   - انظر doctor_appointments_updates.php
   - انظر patient_lab_updates.php
*/

// =============================================================
// النهاية
// =============================================================

/*
هذا النظام يوفر:
✓ ربط تلقائي لجميع العمليات المالية
✓ قيود محاسبية متوازنة
✓ إعدادات قابلة للتخصيص
✓ رسائل توضيحية ذاتية الإخفاء
✓ تقارير مالية شاملة
✓ تتبع كامل للعمليات

البدء الآن:
1. شغل financial_tables.sql
2. عدّل doctor_appointments.php و patient.php
3. جرّب العمليات المختلفة
4. احقق من الأرصدة والقيود
5. استمتع بالنظام المتكامل! 🎉
*/
